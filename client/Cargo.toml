[package]
name = "client"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
rand = "0.9.1"
tokio = { version = "1", features = ["full"] }
crossterm = {version = "0.29.0", features = ["event-stream"]}
futures = "0.3"
